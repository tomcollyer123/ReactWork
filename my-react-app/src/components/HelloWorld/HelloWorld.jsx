// This is my hello world component
// What this function returns is what will be rendered onto the webpage

const HelloWorld = () => { // JSx allows us to combine JS and HTML into one
    return (
        <h3>Hello World</h3> // Can put HTML directly into JS like this
    )
};

// Exporting the component for use
export default HelloWorld